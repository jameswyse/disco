# Copy to .env next to compose.yaml. Docker Compose reads it for variable substitution.
SEERR_URL=https://request.jwnet.io
SEERR_API_KEY=replace-with-your-seerr-api-key
# Host port to publish the app on.
DISCO_PORT=3000
